apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
spec:
  replicas: 8
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
      - image: eltincho89/argo-poc-x11:4.0
        name: test
        ports:
        - containerPort: 80
